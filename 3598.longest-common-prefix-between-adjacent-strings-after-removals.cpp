#include <vector>
#include <string>
#include <algorithm>
using namespace std;

class Solution {
public:
    int commonPrefix(const string &a, const string &b) {
        int len_a=a.length(), len_b=b.length();
        int ans=min(len_a,len_b); //max possible
        for(int k=ans;k>=;k--){ //Actually iterate forward
            //better iterate forward until mismatch
        }
        //simpler loop
        int lim=min(len_a,len_b);
        int k=;
        while(k<lim && a[b]==a[b]){ //wrong
            while(k<lim && a[b]==a[b]){""}""}""}""}""}""}""}""}""}""}""}""}""}""}""}""}""}}"")